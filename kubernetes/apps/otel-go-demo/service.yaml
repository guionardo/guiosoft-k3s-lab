apiVersion: v1
kind: Service
metadata:
  name: otel-go-demo
  namespace: lab
  labels:
    app: otel-go-demo
spec:
  selector:
    app: otel-go-demo
  ports:
    - name: http
      port: 8080
      targetPort: http
