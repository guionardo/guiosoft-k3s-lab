apiVersion: v1
kind: Secret
metadata:
  name: firecrawl-secrets
  namespace: firecrawl
type: Opaque
stringData:
  POSTGRES_USER: "REPLACE_ME"
  POSTGRES_PASSWORD: "REPLACE_ME"
  POSTGRES_DB: "REPLACE_ME"
  # Optional values below should be removed when unused instead of committed with real values.
  # OPENAI_API_KEY: "REPLACE_ME"
  # OPENAI_BASE_URL: "REPLACE_ME"
  # MODEL_NAME: "REPLACE_ME"
  # MODEL_EMBEDDING_NAME: "REPLACE_ME"
  # OLLAMA_BASE_URL: "REPLACE_ME"
  # SLACK_WEBHOOK_URL: "REPLACE_ME"
  # BULL_AUTH_KEY: "REPLACE_ME"
  # TEST_API_KEY: "REPLACE_ME"
  # SELF_HOSTED_WEBHOOK_URL: "REPLACE_ME"
  # PROXY_SERVER: "REPLACE_ME"
  # PROXY_USERNAME: "REPLACE_ME"
  # PROXY_PASSWORD: "REPLACE_ME"
  # SEARXNG_ENDPOINT: "REPLACE_ME"
  # SEARXNG_ENGINES: "REPLACE_ME"
  # SEARXNG_CATEGORIES: "REPLACE_ME"
  # NUQ_BACKEND: "REPLACE_ME"
